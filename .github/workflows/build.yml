name: build
on: pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: np-complete/build-book-action@v2.1
      - uses: actions/upload-artifact@v4
        with:
          name: book
          path: book.pdf
